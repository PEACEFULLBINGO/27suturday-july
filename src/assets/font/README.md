Fonts are loaded from Google Fonts via a `<link>` in `src/index.html`
(Space Grotesk for display, Plus Jakarta Sans for body text). If you need to
self-host for offline-first/PWA reasons, drop the `.woff2` files here and
swap the `<link>` tags for `@font-face` rules in `assets/styles/tokens.css`.
